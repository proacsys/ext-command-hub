# Changelog

## 0.1.1

- README cleanup (removed dev-only sections not relevant to Marketplace listing)

## 0.1.0

Initial release.

- Categories & commands, saved globally across all projects/workspaces
- Export / import (merge or replace) as JSON
- Filter/search by name
- Placeholder variables (`${workspaceFolder}`, `${file}`, etc.)
- Per-command "run in new terminal" and "confirm before running" options
- Drag-to-reorder commands and categories
- `Ctrl+Alt+R` / `Cmd+Alt+R` to re-run the last used command
